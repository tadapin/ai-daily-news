[project]
name = "ai-daily-news"
version = "0.1.0"
description = "Daily AI News generator and GitHub Pages updater"
requires-python = ">=3.11"
dependencies = [
  "feedparser>=6.0.11",
  "openai>=2.0.0",
  "python-dotenv>=1.1.0",
  "requests>=2.32.0",
]

[tool.uv]
package = false
